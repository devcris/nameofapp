
<div>
  <strong>Name:</strong>
  <%= @product.name %>
</div>
<div class="product-image img-responsive">
<%= image_tag(@product.image_url, class: "image-fluid img-thumbnail ", width: "350", data_zoom_image: @product.image_url) unless @product.image_url.nil? %>

</div>
<div>
  <strong>Description:</strong>
  <%= @product.description %>
</div>
<div>
<strong>Rating:</strong>
  <%= @product.highest_rating_comment.rating unless @product.highest_rating_comment.nil? %>
  <i class="fas fa-star"></i>
  <i class="far fa-star"></i>
  <i class="far fa-star"></i>
</div>
<div>
  <strong>Price:</strong>
  <%= @product.price %>
</div>
<%= form_with(url: '/payments/create') do |form| %>
  <%= render partial: "shared/stripe_checkout_button" %>
  <%= hidden_field_tag(:product_id, @product.id) %>
<% end %>
<div>
  <strong>Colour:</strong>
  <%= @product.colour %>
</div>
<strong>Average Rating: <div class="rated" data-score="<%= @product.average_rating %>"></div> </strong>
<%= render 'new_comment' %>
<%= render 'comments' %>
<div class="pagination">
  <center><%= will_paginate @comments %></center>
  </div>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
